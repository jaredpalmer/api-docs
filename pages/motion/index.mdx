<!-- 👋 Editing this file? Need help? → https://github.com/framer/api-docs/blob/master/CONTRIBUTING.md -->

import {
  Template,
  CTA,
  Header,
  EmbeddedVideo,
  EmbeddedDemo,
} from "../../components"

export default Template

<div>

# Framer Motion API

<span className="lead">
  A complete API reference of Framer Motion, a
  production-ready animation and gesture library.
</span>

</div>

<Header />

<div className="followed-by-list">

## Overview

Framer Motion shares the same animation and gesture API as the Framer X library, while maintaining HTML and SVG semantics.

This makes it simple to port your high-fidelity prototypes to a production-ready environment, or make something completely from scratch!

</div>

<div>

- **Animations**:

</div>

```jsx
<motion.div animate={{ x: 100 }} />
```

<div>

- **Gestures**: Blah

</div>

```jsx
<motion.div
  drag="x"
  dragConstraints={{ left: -100, right: 100 }}
/>
```

<div>

- **Variants**:

</div>

```jsx
<motion.ul animate="hidden" variants={list}>
  <motion.li variants={item} />
  <motion.li variants={item} />
  <motion.li variants={item} />
</motion.ul>
```

<div>

- **Imperative controls**: Blah

</div>

```jsx
const controls = useAnimation()

async function sequence() {
  await controls.start({ opacity: 1 })
  return await controls.start({ color: "#0f0" })
}

return <motion.div onClick={sequence} animate={controls} />
```

<div>

- **Server-side rendering**:

</div>

```jsx
<motion.div animate={{ x: 100 }} />
```

---

## Quick Start

### Installation

Install `framer-motion` from npm.

```bash
npm install framer-motion
```

### Importing

Once installed, you can import Framer Motion into your components.

```jsx
import { motion } from "framer-motion"
```

---

## Basics

### Animate

<div>

Balllahg

<CTA name="Learn More" link="/api/examples/#animate" />

</div>

```jsx
// Animate scale to 50%
export function MyComponent() {
  return <Frame animate={{ scale: 0.5 }} />
}
```

---
